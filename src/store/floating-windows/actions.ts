import { ActionTree } from 'vuex';
import { StoreInterface } from '../index';
import { WindowsStateInterface } from './state';

const actions: ActionTree<WindowsStateInterface, StoreInterface> = {
  // someAction(/* context */) {
  //   // your code
  // }
};

export default actions;
